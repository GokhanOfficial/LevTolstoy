
  # Markdown WYSIWYG Editor (Community)

  This is a code bundle for Markdown WYSIWYG Editor (Community). The original project is available at https://www.figma.com/design/hKeib8osqEK8yRFKueefju/Markdown-WYSIWYG-Editor--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  